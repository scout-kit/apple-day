# Refuse to install on a Node the dependencies do not support.
#
# Without this, `engines` is advisory: npm prints a warning nobody reads and installs
# anyway. The failure then arrives much later and looks like nothing to do with Node —
# on the wrong version jsdom mis-handles localStorage, and what you see is two dozen
# storage tests failing with no hint of the cause.
engine-strict=true
